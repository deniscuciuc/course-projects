package Inheritance.calculationapp.calculation;

public class TestCalculationClass extends Calculation {
    public void multiplication(int x, int y) {
        z = x * y;
        System.out.println("The product of the given number is: " + z);
    }
}
